# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new/empty repository. No build system, tests, or application code exists yet.

## Getting Started

When development begins:

1. **Choose and configure the tech stack** before writing code
2. **Set up project structure** following the patterns in `~/.claude/rules/`
3. **Initialize git** if this project will use version control
4. **Configure hooks** for formatting, linting, and type checking per the web rules

## Commands

Commands will be added here as the project is built out. Expected structure:

```bash
# Install dependencies
pnpm install

# Development
pnpm dev

# Build
pnpm build

# Test
pnpm test
pnpm test -- --watch  # single test file

# Lint
pnpm lint
pnpm lint --fix
```

## Architecture

Architecture documentation will be added here as the project takes shape.
